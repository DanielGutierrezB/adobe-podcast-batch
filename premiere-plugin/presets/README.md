# presets/

Poné acá tu preset de export `wav-24-mono-48.epr` (WAV 48 kHz / 24 bit / mono).
Si no existe, el panel busca un preset WAV del sistema o genera uno con Media Encoder.
Cómo crearlo: ver el README del plugin.
